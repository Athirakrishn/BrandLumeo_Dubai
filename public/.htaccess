Options -MultiViews
RewriteEngine On
RewriteBase /

# Serve files and folders that exist
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Fallback to home for unknown routes (SPA-style)
RewriteRule ^ index.html [L]
